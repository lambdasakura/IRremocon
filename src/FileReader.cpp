#include <stdio.h>
#include <string>
#include <fstream>
#include <iostream>
#include <string>

typedef std::string Filename;

const Filename setting = "remocon.dat";

//void read_remocon_data(Filename filename) {
//
//	std::ifstream ifs(filename);
//	std::string str;
//	while (getline(ifs, str)) {
//		std::cout << str << std::endl;
//	}
//}
